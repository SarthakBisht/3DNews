# NewsAPI.org free developer key — https://newsapi.org/register
# Browser requests on the free tier only work from localhost.
# Without this, the app falls back to public/sample-news.json.
VITE_NEWSAPI_KEY=
